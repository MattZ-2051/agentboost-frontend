<!-- <script>
	import { page } from '$app/stores';
	import { loginFx } from '$store/user';
	import { onMount } from 'svelte';

	onMount(async () => {
		const email = $page.url.searchParams.get('user-email');
		if (email) {
			await loginFx({ email, password: 'google' });
		} else {
			window.location.pathname = '/signup';
		}
	});
</script> -->

<h1>here</h1>
